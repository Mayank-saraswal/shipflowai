/**
 * ShipFlow AI — Shared UI Component Library
 *
 * This package will hold shared Shadcn-based components used across
 * the application. For now it's a skeleton — components will be
 * extracted from apps/web/components/ui as we build features.
 *
 * Keeping UI components in a shared package enables:
 * - Consistent design system across the app
 * - Easier testing of components in isolation
 * - Potential reuse in other apps (admin panel, docs site)
 */

// Placeholder export — will be populated as components are extracted
export const UI_VERSION = "1.0.0" as const;
